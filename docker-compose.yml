services:
  frontend:
    image: ghcr.io/aaa-triple-aaa/tripleawebsite:latest
    container_name: triplea_app
    restart: always
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ConnectionStrings__DefaultConnection=Data Source=/app/data/app.db
    volumes:
      - app_data:/app/data
      - uploads_data:/app/wwwroot/uploads

  nginx:
    image: nginx:alpine
    container_name: triplea_nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /opt/tripleawebsite/certbot/conf:/etc/letsencrypt/:ro
      - /opt/tripleawebsite/certbot/www:/var/www/certbot/:ro
    depends_on:
      - frontend

volumes:
  app_data:
  uploads_data:
